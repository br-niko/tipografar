---
layout: default
---

<div class="list-page">
  
  <h1 class="page-title mb-4">{{ page.title }}</h1>

  <div class="row">
    {% assign item_type = page.type %}
    {% assign item_value = page.title | downcase %}

    {% for projeto in site.posts %}
      
      {% assign found = false %}

      {% comment %} 
        Esta é a nova lógica. 
        Ele só vai checar as tags se o tipo da página for 'tag'.
      {% endcomment %}

      {% if item_type == 'category' %}
        {% for cat in projeto.categories %}
          {% if cat | downcase == item_value %}
            {% assign found = true %}
          {% endif %}
        {% endfor %}
      
      {% elsif item_type == 'tag' %}
        {% for tag in projeto.tags %}
          {% if tag | downcase == item_value %}
            {% assign found = true %}
          {% endif %}
        {% endfor %}

      {% elsif item_type == 'semestre' %}
        {% if projeto.semestre == item_value %}
           {% assign found = true %}
        {% endif %}
      {% endif %}

      
      {% comment %} 
        Se a variável 'found' for verdadeira, 
        então nós exibimos o card.
      {% endcomment %}
      {% if found == true %}
        <div class="col-md-4 mb-4">
          <div class="card h-100">
            <div style="height: 200px; background-color: #eee;"></div>
            <div class="card-body">
              <h5 class="card-title">
                <a href="{{ projeto.url }}" class="text-decoration-none">{{ projeto.title }}</a>
              </h5>
              <p class="card-text"><small>{{ projeto.aluno }}</small></p>
            </div>
          </div>
        </div>
      {% endif %}

    {% endfor %}
  </div>

</div>